//Debug line to visualize slope
draw_set_color(c_lime);
draw_line(x, y, sx, sy);

/*
draw_set_color(c_red);
for (var i = 0; i < room_width; i += 10) {
    draw_circle(i, getIntersection(i), 2, true);
}
*/